import { createApp } from 'vue'
import './assets/styles/main.scss'  // agganciato file main.scss come css principale
import * as bootstrap from 'bootstrap' // import bootstrap per JavaScript (da documentazione Bootstrap/Vite)

import App from './App.vue'

createApp(App).mount('#app')
